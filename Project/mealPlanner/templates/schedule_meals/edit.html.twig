{% extends 'base.html.twig' %}

{% block title %}Edit Meals{% endblock %}

{% block body %}

    <div class="container py-4">
    <h1>Edit Meals</h1>

    {{ form_start(form) }}
    {{form_widget(form)}}
    {{ form_end(form) }}
    <a href="{{ path('schedule_meals_index') }}" class=" btn btn-warning">back to list</a>
</div>
{% endblock %}
